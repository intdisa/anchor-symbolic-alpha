training:
  seed: 7
  generator: transformer
  baseline_generator: rnn
  pretraining:
    load_checkpoint: false
    checkpoint: artifacts/checkpoints/synthetic_prior_transformer.pt
    synthetic_examples: 0
    synthetic_epochs: 0
    synthetic_length: 64
  curriculum:
    enabled: false
    initial_max_length: 5
    final_max_length: 5
  stability:
    forbid_nan: true
    gradient_clip_norm: 1.0
