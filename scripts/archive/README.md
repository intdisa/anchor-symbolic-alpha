# Archived Scripts

This directory holds one-off maintenance scripts that were useful during the
mainline migration but are not part of the current experiment workflow.

- `migrate_us_equities_layout.py`: historical layout migration from the old
  pre-mainline directory structure into the current `us_equities` layout
